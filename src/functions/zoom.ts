export function getEntireDomain(){
    // const { data } = props;
    // return {
    //     y: [ data[0][0].y, data[data.length-1][8].y],
    //     x: [ data[0].x, data[data.length-1].x ]
    // };
}