# Open Queries

These questions need product or maintainer decisions before implementation. Engineering details should move to GitHub issues once the intended behaviour is clear.

No open queries at present. Resolved queries: prerelease publishing (`deploy.txt` deleted) and event-fixture placement (removed from the general fixture) were one-off fixes with no lasting roadmap item; the age-display control redirect and export attribution are recorded in `spec/roadmap.md` R6 and R7.
